[build]
builder = "dockerfile"

[deploy]
startCommand = "uv run --prerelease=allow alembic upgrade head && uv run --prerelease=allow uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
